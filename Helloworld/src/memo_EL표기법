JSP - EL(Expression Language)(표현방법,표현식)
- 출력에서 사용하는, 표현하는 언어
예) 출력화면(리스트) , 서버에서 넘어온 리스트 데이터를
반복문, 조건문 이용해서 출력하는 언어.
${list} = TodoDTO의 INSTANCE와 동일 << 이런식으로 쓸 수 있음.
배열처럼도 사용 가능 ${list[0].tno} -> 해당 인스턴스의 게터를 호출하는 효과와 동일함
